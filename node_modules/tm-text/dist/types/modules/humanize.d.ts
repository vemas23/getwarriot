import type { TokenizeOptions } from '../utils/options';
import type { Token } from '../utils/syntax';
export declare const humanize: (input: string | Token[], options?: Partial<TokenizeOptions>) => string;
export default humanize;
